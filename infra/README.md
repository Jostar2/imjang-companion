# Infra Notes

Local infrastructure uses:

- PostgreSQL via `docker-compose.yml`
- MinIO as an S3-compatible object store
- staging deploy contract: `infra/staging/DEPLOYMENT.md`
- production deploy contract: `infra/production/DEPLOYMENT.md`

Production deployment details are intentionally left out of the template and remain subject to staged approval.
